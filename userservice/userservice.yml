spring:
   datasource:
      url: jdbc:postgresql://localhost:5432/testone
      username: devone
      password: devone
      platform: postgresql
      initialization-mode: always
mytest:
   name: murali-alpha

